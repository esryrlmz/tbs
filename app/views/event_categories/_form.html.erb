<%= simple_form_for(@event_category) do |f| %>
  <%= f.error_notification %>  
  <div class="form-inputs">
    <%= f.input :name, label: 'Kategori Adı:' %>
  </div>
  <div class="form-actions">
    <%= f.submit 'Kaydet', :class => "btn btn-success" %>
  </div>
<% end %>
