<%= simple_form_for(@academic_period) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :name, label: "Adı" %>
    <%= f.input :is_active, label: "Aktif mi?" %>
  </div>
  <div class="form-actions">
    <%= f.submit "Kaydet", class: "btn btn-success" %>
  </div>
<% end %>
