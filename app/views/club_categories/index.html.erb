<div class="row">
  <%= render 'home/navbarDefault' %>
</div>
<div class="row">
  <div class="col-md-12">
    <ol class="breadcrumb">
      <li><%= link_to 'Anasayfa', root_path %></li>
      <li class="active">Topluluk Kategorileri</li>
    </ol>
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Topluluk Kategorileri</h3>
      </div>
      <div class="panel-body">
        <p align="right"><%= link_to 'Yeni Topluluk Kategorisi', new_club_category_path, class: "btn btn-default" %></p>
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Name</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @club_categories.each do |club_category| %>
            <tr>
              <td><%= club_category.name %></td>
              <td><%= link_to '<i class="fa fa-eye" title="Göster"></i>'.html_safe, club_category %></td>
              <td><%= link_to '<i class="fa fa-pencil" title="Düzenle"></i>'.html_safe, edit_club_category_path(club_category) %></td>
              <td><%= link_to '<i class="fa fa-trash" title="Sil"></i>'.html_safe, club_category, method: :delete, data: { confirm: 'Topluluk kategorisini silmek istediğinize emin misiniz?' } %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
        <br>
      </div>
    </div>
  </div>
</div>
