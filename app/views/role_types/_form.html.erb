<%= simple_form_for(@role_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Adı" %>
  </div>

  <div class="form-actions">
    <%= f.submit  "Kaydet", class: "btn btn-success" %>
  </div>
<% end %>
